Refatore o componente <componente> do projeto atual para que sua aparência visual, estilos CSS, estrutura HTML e micro-interações fiquem exatamente iguais ao design apresentado na screenshot e no arquivo <arquivo-design>, mantendo toda a funcionalidade existente (event listeners, lógica TypeScript, estados, transições e comportamentos), atualizando apenas os estilos, classes CSS, estrutura de elementos e animações para corresponder precisamente ao design de referência, garantindo que todas as funcionalidades continuem operando corretamente após a refatoração visual.